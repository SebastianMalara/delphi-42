node_name: delphi-42-dev

radio:
  transport: simulated
  device: ""
  channel: 0

privacy:
  answer_public_messages: false
  share_position_publicly: false

broadcasts:
  interval_minutes: 90
  messages:
    - THE ORACLE LISTENS. SEND DM FOR COUNSEL.
    - ASH NODE AWAKE.
    - SEEK WISDOM IN PRIVATE.

knowledge:
  plaintext_dir: sample_data/plaintext
  index_path: artifacts/dev/oracle.db
  kiwix_url: http://kiwix:8080
  zim_dir: sample_data/zim
  runtime_zim_fallback_enabled: false
  runtime_zim_allowlist: []
  runtime_zim_search_limit: 3

llm:
  backend: openai-compatible
  base_url: http://mock-openai:8000/v1
  model: qwen3-1.7B-Int8-ctx-axcl
  api_key: sk-
  timeout_seconds: 45

reply:
  short_max_chars: 120
  continuation_max_chars: 600
  max_continuation_packets: 3

wifi:
  ssid: DELPHI-42-DEV
