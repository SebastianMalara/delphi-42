# Sample Data

This directory contains small repo-tracked fixtures for containerized local development.

- `plaintext/`: tiny grounded corpus used by the local indexer and simulated ask flows

Do not commit large archives, generated indexes, or operator-specific data here.
